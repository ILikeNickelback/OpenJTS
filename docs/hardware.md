# Hardware

OpenJTS drives two hardware components: an MCC DAQ board for high-speed analog acquisition, and an ESP32 for LED sequence timing (see [Firmware](firmware.md)). Design files live under `hardware/`.

```{note}
This page is a skeleton. The `hardware/` subfolders are still empty placeholders — fill in each section below as design files land.
```

## Technical choices

*To do: why this DAQ board, LED driver design, optical layout, and other tradeoffs considered.*

## Bill of materials

*To do — see `hardware/bom/`.*

## Schematics

*To do — see `hardware/schematics/`.*

## PCB

*To do — see `hardware/pcb/`.*

## Enclosure

*To do — see `hardware/enclosure/`.*

## Datasheets

*To do — see `hardware/datasheets/`.*

## Assembly manual

*To do.*
